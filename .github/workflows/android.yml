name: Mixpanel Android Pull Request and master branch CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
    - name: Build all modules
      run: ./gradlew assemble
    - name: Enable KVM
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm
    - name: Run Unit test
      uses: reactivecircus/android-emulator-runner@e89f39f1abbbd05b1113a29cf4db69e7540cae5a  # v2
      with:
        api-level: 34
        arch: x86_64
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
        script: ./gradlew :analytics:createDebugCoverageReport
    - name: Run Session Replay instrumentation tests
      uses: reactivecircus/android-emulator-runner@e89f39f1abbbd05b1113a29cf4db69e7540cae5a  # v2
      with:
        api-level: 34
        arch: x86_64
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
        script: ./gradlew :session-replay:connectedDebugAndroidTest
    - name: Run OpenFeature Provider tests
      run: ./gradlew :openfeature-provider:test
    - name: Run Common module tests
      run: ./gradlew :common:test
    - name: Run Session Replay unit tests
      run: ./gradlew :session-replay:test
    - name: Run Common ktlint check
      run: ./gradlew :common:ktlintCheck
    - name: Run Session Replay ktlint check
      run: ./gradlew :session-replay:ktlintCheck
    - name: Lint
      run:  ./gradlew :analytics:lint
    - name: Run Session Replay lint
      run: ./gradlew :session-replay:lint
    - name: Upload test report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Unit Test Report
        path: analytics/build/reports/androidTests/connected
    - name: Upload OpenFeature test report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: OpenFeature Unit Test Report
        path: openfeature-provider/build/reports/tests/
    - name: Upload Common test report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Common Unit Test Report
        path: common/build/reports/tests/
    - name: Upload Common ktlint report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Common ktlint Report
        path: common/build/reports/ktlint/
    - name: Upload Session Replay test report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Session Replay Unit Test Report
        path: session-replay/build/reports/tests/
    - name: Upload Session Replay ktlint report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Session Replay ktlint Report
        path: session-replay/build/reports/ktlint/
    - name: Upload Session Replay lint report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Session Replay Lint Report
        path: session-replay/build/reports/lint-results-*.*
    - name: Upload Session Replay instrumentation test report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      if: always()
      with:
        name: Session Replay Instrumentation Test Report
        path: session-replay/build/reports/androidTests/connected
    - name: Upload test coverage report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      with:
        name: Test Coverage Report
        path: analytics/build/reports/coverage/debug/
    - name: Upload lint report
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
      with:
        name: Lint Report
        path: analytics/build/reports/lint-results-*.*
    - name: Android docs
      run:  ./gradlew --info :analytics:androidJavadocs

